<UserControl x:Class="SpotifyEditor.View.Board"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:collections="clr-namespace:System.Collections.Generic;assembly=System.Collections"
             xmlns:local="clr-namespace:SpotifyEditor.View"
             xmlns:common="clr-namespace:SpotifyEditor.View.Nodes.Common"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1540"  >


    <UserControl.Resources>
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FF333333"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="DemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid>

                            <Border x:Name="Border" 
                            BorderBrush="Black"
                                BorderThickness="1"
                            Background="{TemplateBinding Background}" 
                            CornerRadius="4" 
                            Padding="{TemplateBinding Padding}">
                                <Grid >
                                    <ContentPresenter x:Name="contentPresenter"
                                                  
                                              ContentSource="Header" 
                                              HorizontalAlignment="Left" 
                                              VerticalAlignment="Center"/>

                                    <Path x:Name="Arrow"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  Margin="160,0,0,0"
                                  Width="8"
                                  Height="8"
                                  Fill="White"
                                  Data="M 0 0 L 4 4 L 0 8 Z"
                                  Visibility="Collapsed"/>
                                </Grid>
                            </Border>


                            <Popup x:Name="PART_Popup" 
                               
                           Placement="Right" 
                           IsOpen="{TemplateBinding IsSubmenuOpen}" 
                           AllowsTransparency="True" 
                           Focusable="False" 
                           PopupAnimation="Fade">
                                <Border Background="#FF333333"
                                BorderBrush="White" 
                                BorderThickness="3"  
                                CornerRadius="4" 
                                Padding="2" 
                                SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF555555"/>

                            </Trigger>


                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF777777"/>
                            </Trigger>



                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid >
        <Grid.ContextMenu>
            <ContextMenu x:Name="NodeContextMenu"
                         StaysOpen="True"
                         Placement="MousePoint" 
                         Opened="NodeContextMenu_Opened"/>
        </Grid.ContextMenu>


        <Rectangle x:Name="BoardBackground"  Style="{StaticResource BoardBackground}" />
        <Canvas x:Name="BoardForeground"  x:FieldModifier="public" ClipToBounds="True"/>
    </Grid>
</UserControl>
