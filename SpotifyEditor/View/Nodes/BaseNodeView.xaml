<UserControl x:Class="SpotifyEditor.View.Nodes.BaseNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpotifyEditor.View.Nodes"
             xmlns:ports="clr-namespace:SpotifyEditor.View.Nodes.Ports"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent"
             MouseLeftButtonDown="OnMouseLeftButtonDown"
             MouseMove="OnMouseMove"
             MouseLeftButtonUp="OnMouseLeftButtonUp"
             >
    <Grid Width="300" Height="200" Background="WhiteSmoke">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="15"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="{Binding Name}" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="20" FontSize="20"/>

        <TextBlock Text="Input Count" Grid.Column="0" VerticalAlignment="Center" Margin="20" FontSize="15" TextWrapping="Wrap"/>
        <TextBlock Text="Output Count" Grid.Column="2" VerticalAlignment="Center" Margin="20" FontSize="15" TextWrapping="Wrap"/>


        <StackPanel x:Name="inputPortContainer" Grid.Column="0" VerticalAlignment="Center">
            <ports:InputPortView  />
            
        </StackPanel>

        <StackPanel x:Name="outputPortContainer" Grid.Column="2" VerticalAlignment="Center">
            <ports:OutputPortView  />

        </StackPanel>

        <TextBlock  Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Center"
                    Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}" Margin="50" FontSize="30"/>

        <Rectangle Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Center"
                   Fill="{Binding ExecuteIndicatorColor, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="20" />

    </Grid>
    
</UserControl>
