<UserControl x:Class="SpotifyEditor.View.Nodes.Common.TimePeriodSliderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpotifyEditor.View.Nodes.Common"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
    <Slider Grid.ColumnSpan="3" Background="AliceBlue" 
            Minimum="0" Maximum="2" Style="{StaticResource ModernSlider}"
            IsSnapToTickEnabled="True"
            Value="{Binding TimeDuration, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Column="0" Grid.Row="1" Foreground="White" HorizontalAlignment="Left" Text="4 Weeks"/>
        <TextBlock Grid.Column="1" Grid.Row="1" Foreground="White" HorizontalAlignment="Center" Text="6 Months"/>
        <TextBlock Grid.Column="2" Grid.Row="1" Foreground="White" HorizontalAlignment="Right" Text="1 Year"/>
    </Grid>

</UserControl>
