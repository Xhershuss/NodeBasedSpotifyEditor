<UserControl x:Class="SpotifyEditor.View.Nodes.Common.ScoreSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SpotifyEditor.View.Nodes.Common"
             mc:Ignorable="d"
             d:DesignHeight="45" d:DesignWidth="800">

 

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Slider Grid.ColumnSpan="3" Style="{StaticResource ModernSlider}"
                Background="Transparent"  Minimum="1" Maximum="100" 
 TickFrequency="1"
                Value="{Binding PopularityScore, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Column="0" Grid.ColumnSpan="3"
                   Grid.Row="1" Foreground="White" FontSize="16"
                   HorizontalAlignment="Center"
                   Text="{Binding PopularityString}"/>
    </Grid>
</UserControl>