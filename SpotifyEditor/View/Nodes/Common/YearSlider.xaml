<UserControl x:Class="SpotifyEditor.View.Nodes.Common.YearSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpotifyEditor.View.Nodes.Common"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="800">
    <StackPanel>

        <TextBlock Text="Select Min Date:" Foreground="White" FontSize="12" HorizontalAlignment="Left" Margin="0,0,0,3"/>
        <Slider Grid.ColumnSpan="3" Background="AliceBlue" 
                Style="{StaticResource ModernSlider}"
            Minimum="1940" Maximum="2025"  
            TickFrequency="1"
            Value="{Binding MinDate, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Select Max Date:" FontSize="12" Foreground="White"  HorizontalAlignment="Left" Margin="0,0,0,3"/>
        
        <Slider Grid.ColumnSpan="3" Background="AliceBlue"
         Style="{StaticResource ModernSlider}"
         Minimum="1940" Maximum="2025" 
         TickFrequency="1"
         Value="{Binding MaxDate, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="{Binding DateString , NotifyOnSourceUpdated=True}" Foreground="White"  Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" HorizontalAlignment="Center" FontSize="16" FontWeight="DemiBold" Margin="0,-5  ,0,0" />

        </StackPanel>

</UserControl>
