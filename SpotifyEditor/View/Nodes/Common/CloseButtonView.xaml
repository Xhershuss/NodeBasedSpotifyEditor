<UserControl x:Class="SpotifyEditor.View.Nodes.Common.CloseButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpotifyEditor.View.Nodes.Common"
             mc:Ignorable="d" >
    <Grid>

        <Button x:Name="CloseButton" 
                Width="20" 
                Height="20" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Margin="5"
                Background="Red"
                Foreground="White"
                FontWeight="Bold"
                Content="X"
                Command="{Binding ViewModelReference.RemoveNode}" 
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext}">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            CornerRadius="5"    
                        BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
