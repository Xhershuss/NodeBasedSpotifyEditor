<Window x:Class="SpotifyEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpotifyEditor"
        xmlns:vm="clr-namespace:SpotifyEditor.ViewModel.Nodes"
        xmlns:View="clr-namespace:SpotifyEditor.View"
        mc:Ignorable="d"
        WindowState="Maximized"
        ResizeMode="CanMinimize"
        WindowStyle="None"
        UseLayoutRounding="True" 
         SnapsToDevicePixels="True"
        Title="MainWindow" Height="1080" Width="1920">

    <Window.Resources>
        <Style TargetType="Button" x:Key="TriggerButtonStyle" BasedOn="{StaticResource ModernButton}">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                        
                            <ContentPresenter x:Name="CP"
                                          Content="{TemplateBinding Content}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter  Property="Background" Value="#FF3A3A3A"/>
                    <Setter  Property="BorderBrush" Value="#FF5A5A5A"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#222224"/>
                    <Setter Property="Foreground" Value="WhiteSmoke"/>
                    <Setter Property="BorderBrush" Value="Gray"/>
                </Trigger>
             
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition />
        </Grid.RowDefinitions>
    
        <TextBlock Text="NB Spotify Editor"
               VerticalAlignment="Center"
               Margin="10,0,0,0"
               Foreground="White"
               FontSize="16"/>


    <StackPanel Orientation="Horizontal"
                Grid.Column="1"
                VerticalAlignment="Center" HorizontalAlignment="Right">

            <Button Content="―" Width="50" Height="30" Click="Minimize_Click">
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border x:Name="Border" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2c2c2c" TargetName="Border" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="✕" Width="50" Height="30" Click="Close_Click">
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border x:Name="Border" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2c2c2c" TargetName="Border" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>



        
    </StackPanel>

    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="205"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>



        <Rectangle Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Fill="{StaticResource LeftColumnBrush}" Panel.ZIndex="0" />
        <Rectangle Grid.Column="1" Grid.Row="0" Grid.RowSpan="3" Fill="{StaticResource CenterColumnBrush}" Panel.ZIndex="0" />
        <Rectangle Grid.Column="2" Grid.Row="0" Grid.RowSpan="3" Fill="{StaticResource RightColumnBrush}" Panel.ZIndex="0" />

 
        <Rectangle Grid.Column="1" Grid.Row="0" Fill="{StaticResource HeaderBrush}" Panel.ZIndex="1" />



            <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,0,0,0">


                <Button x:Name="TriggerButton"
        Command="{Binding TriggerExecution}"
        
        Content="Execute"
        Width="180" Height="70"
        FontSize="40"
        BorderThickness="5" Style="{StaticResource TriggerButtonStyle}"/>





                <StackPanel  Margin="0,400" HorizontalAlignment="Left" Orientation="Vertical">

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Command="{Binding ResetNodes}" HorizontalAlignment="Right" Content="Reset" Width="130" Height="40" Style="{StaticResource ModernButton}" />
                        <Image Source="pack://application:,,,/Assets/Icons/ResetIcon.png" Margin="10,0,0,0" Width="40" />
                    </StackPanel>


                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                        <Button Command="{Binding ClearNodes}" HorizontalAlignment="Right" Content="Clear" Width="130" Height="40" Style="{StaticResource ModernButton}" />
                        <Image Source="pack://application:,,,/Assets/Icons/ClearIcon.png" Margin="10,0,0,0"  Width="40" />
                    </StackPanel>
                    
                </StackPanel>

            </StackPanel>

            <StackPanel Grid.Column="1" Grid.RowSpan="2" Grid.Row="0"  Panel.ZIndex="2" HorizontalAlignment="Right">
                
                <StackPanel  Orientation="Horizontal" Margin="10">
                    <Image Source="pack://application:,,,/Assets/Icons/SaveIcon.png" Width="40"/>
                    <Button Command="{Binding SaveNodes}" Content="Save" Width="130" Margin="0,0,10,0" Height="40" Style="{StaticResource ModernButton}" />
                    <Button Command="{Binding LoadExistingNodes}" Content="Load" Width="130" Height="40" Style="{StaticResource ModernButton}" />
                </StackPanel>
                
            </StackPanel>

        <View:Board x:Name="BoardControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="1" Grid.Row="1" />

        </Grid>



    </Grid>
</Window>
